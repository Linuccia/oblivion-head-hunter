
spring.datasource.url=jdbc:postgresql://localhost:5432/oblivion
spring.datasource.username=postgres
spring.datasource.password=uhm562

spring.liquibase.url=${spring.datasource.url}
spring.liquibase.user=${spring.datasource.username}
spring.liquibase.password=${spring.datasource.password}
spring.liquibase.change-log=classpath:db.migration/db.changelog-master.yaml
spring.liquibase.database-change-log-table=LIQ_DATABASECHANGELOG
spring.liquibase.database-change-log-lock-table=LIQ_DATABASECHANGELOGLOCK
spring.liquibase.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=none
